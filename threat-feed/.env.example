# FireClaw Threat Feed API - Environment Variables
# Copy to .env for local development overrides

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Database (override config.yaml if needed)
# DB_PATH=./data/fireclaw.db
